# Normalize all text files to LF in the repo; check out with native endings.
* text=auto eol=lf

# Windows batch scripts must use CRLF to run reliably in cmd.exe.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
